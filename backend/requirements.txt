fastapi>=0.110
uvicorn[standard]>=0.29
anthropic>=0.40
pydantic[email]>=2.6
python-multipart>=0.0.9    # file uploads (blood work reports)
pillow>=10.0               # downscale large photo uploads before vision extraction

# Persistence
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.19        # dev/default driver (SQLite)
asyncpg>=0.29          # prod driver (Postgres) — used when DATABASE_URL is postgres
alembic>=1.13          # migrations (before prod schema changes)

# Auth
argon2-cffi>=23.1      # password hashing
pyjwt>=2.8             # JWT access tokens
